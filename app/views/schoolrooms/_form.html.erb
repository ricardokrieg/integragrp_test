<%= form_for(@schoolroom) do |f| %>
  <% if @schoolroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schoolroom.errors.count, "error") %> prohibited this schoolroom from being saved:</h2>

      <ul>
      <% @schoolroom.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
